# ---------------------------------------------------------------------------
#  exness-bot - Python dependencies
#
#  Install:   pip install -r requirements.txt
#
#  Works on Python 3.8 (Windows 7) through Python 3.12 (Windows 10 / 11).
#  pip automatically picks the newest release compatible with your Python,
#  so you do NOT need to change anything here.
# ---------------------------------------------------------------------------

MetaTrader5>=5.0.37        # connect to the MT5 terminal, pull candles, send orders (Windows only)
pandas>=1.3.5              # indicator maths + the backtester
numpy>=1.21               # required by pandas; listed so old-Python wheels resolve cleanly
openai>=1.10,<2           # OPTIONAL - only used when OPENAI_API_KEY is set in settings.py
